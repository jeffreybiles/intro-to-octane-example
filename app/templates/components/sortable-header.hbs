<div class="table-cell header {{if isSelected 'selected'}}" {{action @sortBooks @sortBy}}>
  {{@columnName}}
  {{#if isSelected}}
    {{#if @isReversed}}
      &uarr;
    {{else}}
      &darr;
    {{/if}}
  {{/if}}
</div>
